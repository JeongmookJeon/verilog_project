

typedef enum{
	HOUR,
	MIN,
	SEC,
	MSEC,
}watch_state_t;

uint16_t w_counter = 0;

void Watch_Init()
{
	FND_Init();
	w_counter = 0;
}

// digit의 freq 빨라서 1개씩 출력하는 함수
void Watch_DispLoop(){
	FND_DispDigit();
}

//sec? msec? 수준의 counter
void Watch_Excute() {

}

void Watch_Speed(){
	static uint32_t prevTimeCounter = 0;
	if (millis() - prevTimeCounter < 100 - 1) {
	return;
	}
	prevTimeCounter = millis();
	FND_SetNum(counter++);
}
