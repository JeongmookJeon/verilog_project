#include "UpCounter.h"

void UpCouter_Init()
{
	FND_Init();
}

void UpCounter_Excute()
{
	uint16_t counter = 0;
	uint32_t prevTimeCounter = 0;

		if(millis() - prevTimeCounter >= 100){
			prevTimeCounter = millis();
			FND_SetNum(counter++);
		}
		FND_Disp_Digit();

}
